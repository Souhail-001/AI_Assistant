version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - APP_NAME=AI Career Assistant Platform
    env_file:
      - .env
    restart: unless-stopped
